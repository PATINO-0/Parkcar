name: parkcar
description: Simple parking app with SQLite (relational) + Hive (non-relational)
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.3+1           
  path: ^1.9.0
  hive_flutter: ^1.1.0        
  intl: ^0.19.0
  sembast: ^3.5.0             
  sembast_web: ^2.3.0         

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
